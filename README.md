# Focus Timer

Pomodoro timer CLI dengan session tracking dan daily stats.

## Requirements

- Python 3.8+
- Windows (native) / Linux/macOS (dengan fallback)

## Install

pip install -r requirements.txt

## Run

python main.py
